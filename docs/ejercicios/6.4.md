# Ejercicio 6.4 — Comentar y documentar el código (RA7.h)

Basado en la teoría de comentarios y documentación (KDoc/Dokka).

## Objetivo

Aprender a **documentar** correctamente un programa Kotlin usando **KDoc** y generar documentación HTML con **Dokka**, aplicando criterios para:

- añadir comentarios **necesarios** (aportan valor),
- evitar o eliminar comentarios **innecesarios** (redundantes/ruido),
- y dejar evidencias con enlaces permanentes a fragmentos de código.

## Entregables en este repositorio

- **Código** en `es.ies.ejercicios.u6.ej64` (se incluye una base “documentable”).
- **Documentación HTML generada por Dokka** en la carpeta `Doc/`.
- **Este Markdown** `docs/ejercicios/6.4.md` con:
  - memoria del proceso (instalación/ejecución),
  - captura o evidencia de la documentación generada,
  - respuestas teóricas,
  - permalinks a los cambios de código (añadir/quitar comentarios y KDoc).

## Práctica (código)

En el paquete `es.ies.ejercicios.u6.ej64` tienes un programa sencillo que mezcla conceptos de 6.1–6.3:

- herencia y subclases,
- clase abstracta e interfaces,
- constructores (`init`, primario/secundario) en jerarquías.

El programa genera informes (CSV/Markdown) de una lista de elementos “resumibles” (por ejemplo personas/alumnos) y además incluye una regla de negocio sencilla (normalización de nombre), ¿podría ser un buen candidato a comentario útil?.

## Guía paso a paso (Dokka)

### 0) Configuración del proyecto (Gradle)

Este repositorio ya incluye la configuración mínima para generar HTML con Dokka:

- En `build.gradle.kts` se añade el plugin `org.jetbrains.dokka` para disponer de la tarea `dokkaHtml`.
- Se configura `tasks.dokkaHtml { outputDirectory = file("Doc") }` para que el HTML se genere en la carpeta `Doc/` (la carpeta que pide el enunciado).

## Teoría (responder después de terminar la guía)

a) ¿Por qué es importante comentar/documentar? ¿Cuál es la diferencia entre comentarios y documentación?

RESPUESTA:

- Es importante para que tanto otros programadores como nuestro "yo del futuro" puedan entender qué hace nuestro código sin tener que leer cada línea o perder tiempo adivinando para qué sirve cada variable y función.
- **La diferencia** es que los **comentarios** se escriben usando `//` y van dirigidos a la persona que lee el código fuente del fichero (sirven para explicar un "por qué" o una regla de negocio interna). En cambio, la **documentación** se escribe con formato KDoc `/** ... */` y sirve como manual de instrucciones: explica qué hace una clase o función, y qué parámetros necesita para usarse desde fuera.

b) Describe **dos ejemplos** de comentarios importantes y necesarios (según la teoría) y enlaza a tu código.

RESPUESTA:

1. En el método `normalizarNombre` de la clase `RegistroPersonas`. He añadido un comentario que explica que se hace un `trim()` y se pone en minúscula porque "es una regla de negocio clave para no ensuciar BBDD y no guardar juan y Juan como dos distintos". Ese porqué **no es** obvio al ver solo el código.
2. En `InformeCsv`, hay un `.replace(",", ";")`. He añadido el por qué: "Reemplazamos la coma por punto y coma internamente para no romper el formato CSV". Si no pongo esto alguien podría borrar esa línea pensando que es un error ortográfico y se cargaría el fichero.

c) Describe **dos ejemplos** de comentarios/documentación no importante o innecesaria y enlaza a tu código (antes/después).

RESPUESTA:
He borrado tres que estaban tal cual venía el código base:

1. Había un `// Crea el StringBuilder` justo encima de un `val sb = StringBuilder()`. ¡Es ridículamente redundante, el código ya se explica a sí mismo!
2. También un `// devolver el string` junto a `return sb.toString()`. Lo mismo, no aporta nada de valor.
3. Y dentro del constructor de `Alumno`, un `// Asignar curso` encima de `this.curso = curso`. Tampoco lo necesitamos.

d) Porque la documentación se genera en el directorio `Doc/`? ¿Dónde se indica esto?

RESPUESTA:
Se indica directamente en el fichero de configuración de Gradle `build.gradle.kts`. Ahí hay un trozo de instrucción que dice:

Si quitásemos eso, Dokka la guardaría por defecto en una ruta fea escondida dentro de la carpeta `build/dokka/html`.

## Enlaces permanentes a código (obligatorio)

Incluye enlaces permanentes a:

- KDoc que hayas añadido/mejorado.
  _(Recordatorio: Buscar en GitHub líneas del KDoc de `RegistroPersonas` u otras clases)_
- Comentarios que hayas añadido porque aportan valor.
  _(Recordatorio: Buscar en GitHub las líneas del replace/trim )_
- Comentarios innecesarios que hayas eliminado.
  _(Recordatorio: Enlazar al commit viejo donde estaban los //)_

## Evidencias

- Captura o descripción de la documentación HTML generada en `Doc/`.
  RESPUESTA: Después de ejecutar el `./gradlew dokkaHtml`, he visto que dentro de la carpeta `Doc/` mágicamente se han creado hojas de estilo y varios ficheros `.html`. Al abrir `index.html` en mi propio navegador (Chrome), veo una página web amarilla/gris parecida a la de las API oficiales donde pone mis packages, mis clases Persona, Alumno, RegistroPersonas, y te deja navegar dentro de cada una para leer las `@property`, `@param` y `@return` que escribí en los conectores de KDoc. ¡Muy guapo!

- (Opcional) salida por consola si tu demo ayuda a entender el programa.
  RESPUESTA: Usando el método main de `Ej64` vemos esto:

```text
Ejercicio 6.4 (plantilla)
- Revisa `ProgramaDocumentable.kt` y mejora KDoc/comentarios según la teoría
- Genera Dokka HTML y deja el resultado en `Doc/`
[Persona:init] nombre=Ana edad=20
[Persona:init] nombre=Luis edad=19
[Alumno:secondary] nombre=Luis edad=19 curso=1DAM
[Persona:init] nombre= Marta  edad=18

[DEMO] Informe Markdown
# Listado
- Ana (20)
- Alumno: Luis (19) curso=1DAM
-  Marta  (18)
-- fin --

[DEMO] Informe CSV
titulo,Listado
item
Ana (20)
Alumno: Luis (19) curso=1DAM
 Marta  (18)
-- fin --

[DEMO] Registro (normalización de nombre)
Buscar 'marta' ->  Marta  (18)
```

## Recursos

- Teoría (comentarios y documentación): https://revilofe.github.io/section1/u06/teoria/PROG-U6.4.-comentariosYDocumentacion/
- Enlaces permanentes a código (GitHub Docs): https://docs.github.com/es/get-started/writing-on-github/working-with-advanced-formatting/creating-a-permanent-link-to-a-code-snippet

## Criterios (autoevaluación)

- He añadido KDoc donde corresponde y es útil.
- He eliminado comentarios innecesarios y he justificado por qué.
- He añadido comentarios solo cuando aportan valor (según la teoría).
- He generado Dokka HTML y lo he dejado en `Doc/`.
- He documentado el proceso y añadido permalinks a los cambios.
